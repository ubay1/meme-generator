import { StyleSheet } from "react-native";

export const styleBottomSheet = StyleSheet.create({
  container: {
    marginHorizontal: 10,
    flexDirection: "column",
    gap: 4,
    marginTop: 10,
    borderBottomWidth: 1,
    paddingBottom: 10,
  },
});